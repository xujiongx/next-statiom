export const metadata = {
  title: 'OpenRouter 聊天 | AI工具箱',
  description: '使用 OpenRouter API 进行对话，体验 GPT-4o 等多种模型',
};

export default function OpenRouterChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}